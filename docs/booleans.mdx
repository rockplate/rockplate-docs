# Booleans

```rockplate:tabs
{
  "order": {
    "paid": true,
    "total": "$450.00"
  }
}

Total: [order total]

[if order is paid]
  Paid
[else]
  Unpaid
[end if]
```

## Operators

`is`
`is not`
`are`
`are not`
